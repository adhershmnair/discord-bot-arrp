# Alternate Reality RolePlay.

Node version >= ```v18.20.4```

Install yarn

```npm install -g yarn```

Install Packages

```yarn install```


Copy ```.env.example``` file and rename to ```.env```

Update ```src/configuration/discordConfigs.js``` file with proper discord roles and channel ids.

Run server

```yarn run dev```
